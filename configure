#!/bin/bash
DIR=$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )
set -e
set -o pipefail
cd $DIR
cd build
cmake clean ../
make clean
cmake ../
make -j 8
echo "Please add /usr/share/lib and the build folder to your PYTHONPATH"  
echo "running tests"		
../run_tests
 
